apiVersion: v1
kind: Service
metadata:
  labels:
   app: result
  name: result
  namespace: vote
spec:
  type: NodePort
  ports:
  - name: "result-service"
    port: 5001
    targetPort: 80 # Application
    nodePort: 31001 # Exposure to outside Cluster
  selector:
    app: result